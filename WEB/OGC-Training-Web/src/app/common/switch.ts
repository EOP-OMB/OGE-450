export class Switch {
    value: boolean = false;
    onText: string;
    offText: string;

    color: string;
    offColor: string;

    size: string; // sizes = ["mini", "small", "normal", "large"];

    disabled: boolean;

    switchInverse: boolean = true;
}